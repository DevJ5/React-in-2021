{
  "name": "typescript-cheatsheet",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -w",
    "dev": "yarn concurrently -k -n COMPILER,NODEMON -c yellow,blue \"tsc -w\" \"nodemon -q dist -w\""
  },
  "devDependencies": {
    "concurrently": "^6.3.0",
    "nodemon": "^2.0.13",
    "typescript": "^4.4.3"
  }
}
